# Metadata for nih-plug's `cargo xtask bundle` command.
# Maps the cargo package name to a human-readable bundle name, so the output is
# "Codec.vst3" / "Codec.clap" instead of "codec.*".
[codec]
name = "Codec"
